#include <stdio.h>

int main() {
  // we initialize the `num` variable
  int num = 567;

  // we print the number in three different ways according to the question
  printf("%06d\n", num);
  printf("%04d\n", num);
  printf("%d\n", num);

  return 0;
}
