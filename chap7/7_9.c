#include <stdio.h>

int main() {
  // calculate the sum of the series
  double result = (1.0 / 3.0) + (3.0 / 7.0) - (5.0 / 11.0) + (7.0 / 15.0);

  // print the sum
  printf("\nThe result is: %lf", result);

  return 0;
}
